const ProjectBar = () => {
    const el_bar = document.createElement("div");
    el_bar.classList.add("project-bar")
    
    // TODO: Add project list
    
    return el_bar;

}

export default ProjectBar;
